# Структура проекта Online Store (Task 2)

Этот проект — ваш интернет-магазин. Весь основной код находится в папке `src/app`.

## 📂 Папки и Файлы

### 1. `models/` (Модели данных)

- **Файл:** `product.model.ts`
- **Что это:** Описание того, как выглядит "Товар". Тут указано, что у каждого товара есть `id`, `name`, `price` и так далее.
- **Зачем:** Чтобы TypeScript проверял ошибки. Если вы попробуете использовать свойство, которого нет в модели, он подчеркнет красным.

### 2. `data/` (Данные)

- **Файл:** `products.ts`
- **Что это:** Список всех товаров в магазине.
- **Как добавить товар:** Просто добавьте новый объект в массив `products` в этом файле.
- **Зачем:** Заменяет базу данных. Мы просто берем данные из этого файла и показываем на экране.

### 3. `components/` (Компоненты)

Тут лежит внешний вид (HTML) и логика (TS) для разных частей страницы.

#### `product-list/` (Список товаров)

- Отвечает за сетку товаров на главной странице.
- Получает данные из `data/products.ts` и в цикле (`*ngFor`) создает много карточек.

#### `product-card/` (Карточка товара)

- Отвечает за один конкретный прямоугольник с товаром.
- Содержит:
  - Картинку (и галерею картинок).
  - Название, цену, рейтинг.
  - Кнопки "Поделиться" (WhatsApp, Telegram).
- Логика переключения картинок находится в `product-card.component.ts`.

### 4. Главные файлы

- **`app.component.html`**: Главная обертка сайта. Тут находится шапка (Header) и `<router-outlet>`, куда подставляется список товаров.
- **`app.routes.ts`**: Настройка маршрутов. Тут написано, какой компонент показывать, когда приложение запускается.

## 🐳 Docker

Если вы запускаете через Docker (`docker-compose up`):

- Этот проект (`task2`) доступен по адресу: **http://localhost:4202**
