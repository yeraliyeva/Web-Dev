# Task 1: Официальный Туториал Angular

В этом задании вы должны пройти официальный урок от Angular.

## 🔗 Ссылка на урок

[Angular Tutorial: Homes App](https://angular.dev/tutorials/learn-angular)

Обязательно пройдите его по шагам! Вам нужно будет создавать новые компоненты.

## 📂 Где писать код?

Вся работа происходит в папке:
`d:\6.Web\Web-Dev\Lab4\task1\src\app`

### Как проверить, что работает?

1.  **Запуск:**
    Откройте терминал в папке `task1` и напишите:

    ```bash
    npm start
    ```

    (Или используйте Docker: `docker-compose up`, тогда адрес будет `localhost:4201`)

2.  **Где создавать файлы:**
    В туториале вас попросят создать компонент `HousingLocation`.
    Вы должны будете создать папку:
    `src/app/housing-location/`
    И в ней файлы:
    - `housing-location.component.ts`
    - `housing-location.component.html`
    - `housing-location.component.css`

    Или использовать команду Angular CLI (быстрее и проще):

    ```bash
    ng generate component housing-location --inline-template --inline-style
    ```

    (Туториал может использовать `--inline-template`, обратите внимание на флаги в уроке).

### Как понять, что я все сделал правильно?

Если вы пройдете урок до конца, у вас на экране (`localhost:4200` или `4201`) будет:

1.  Шапка с логотипом.
2.  Поле поиска (Search).
3.  Список карточек домов (Housing Locations).
4.  При клике на карточку — переход на страницу деталей (Details Page).
